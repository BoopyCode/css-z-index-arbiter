# CSS Z-Index Arbitrator

Stops the z-index arms race by providing sensible, named layers instead of random huge numbers. Diagnoses CSS files to find z-index crimes. Run: `node z-index-arbitrator.js` for usage.